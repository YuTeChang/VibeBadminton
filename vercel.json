{
  "installCommand": "npm install --registry https://registry.npmjs.org/",
  "buildCommand": "npm run build",
  "framework": "nextjs",
  "ignoreCommand": "bash -c 'if git log -1 --pretty=%B | grep -q \"\\[deploy\\]\"; then exit 1; else exit 0; fi'"
}

